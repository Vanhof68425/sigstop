Now the reply. The source asked for one exact line, and my draft
stops where the ending belongs.

> **Tonight's tools**
> `i` — from command state: enter insert state. Now your keys are text.
> `Esc` — back to command state
> `:wq` — from command state: save the file and leave ("write, quit")
> `cat <file>` — print a file, to check your work from outside

**This step: finish the reply and save it.**

1. Open it: `vim /var/sigstop/drop/reply.draft`
2. Arrow down to the last line — the empty one after my sentences.
3. Press `i` to enter insert state, and type the ending exactly:

   ```
   we act.
   ```

   With the period. Precision is identity.
4. Press `Esc`, then type `:wq`, then Enter. That saves and leaves.
5. Check from outside: `cat /var/sigstop/drop/reply.draft` and read
   what you wrote. My sentences stay as written — the ending is the
   only thing you add.

Then CHECK. No codeword tonight. The reply is the signal.

<details>
<summary>&gt; ping handler</summary>

You're typing and nothing is appearing — or colons are appearing in
your text. State. Always state. `i` puts you where keys are text;
`Esc` puts you where keys are orders; `:wq` only works from orders.
And if the file looks wrong after a struggle: `Esc`, `:q!`, and the
draft is back the way I left it. Then walk in again, calmer.

</details>

<details>
<summary>&gt;&gt; ping again</summary>

`vim` the draft, arrows to the bottom, `i`, type the line — three
words you've seen on every MOTD since World 1, plus the period —
`Esc`, `:wq`, Enter. If you mangle something mid-edit, `Esc` then
`:q!` discards everything and you start the step clean. The file
only keeps what `:wq` saves.

</details>

<details>
<summary>&gt;&gt;&gt; just tell me</summary>

Fine.

```
vim /var/sigstop/drop/reply.draft
```

Inside: arrow to the last line, `i`, type `we act.` — with the
period — then `Esc`, `:wq`, Enter. Then:

```
cat /var/sigstop/drop/reply.draft
```

Say the doors back to me: `i` in. `Esc` out. `:wq` keep. `:q!`
burn. Four keys against the fear of a generation. Last time I
explain them.

</details>
