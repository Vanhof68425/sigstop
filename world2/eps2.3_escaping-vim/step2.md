The message first. You read it, and you read it *in the theater*.

> **Tonight's tools**
> `vim <file>` — open a file in vim
> vim has two states. **Command**: keys are orders. **Insert**: keys are text.
> `Esc` — return to command state, from anywhere
> `:q!` — from command state: leave WITHOUT saving. Any accidental change is discarded.
> Arrow keys move you around in both states.

**This step: read the transmission and leave it untouched.**

1. Open it: `vim /var/sigstop/drop/transmission_07.msg`
2. Read it. Arrow keys to move. Don't worry about pressing something
   wrong — the exit below discards mistakes.
3. Leave without saving: press `Esc`, then type `:q!`, then Enter.

The original goes to the vault later, byte for byte, exactly as it
arrived. `:q!` is the only exit that guarantees you changed
nothing — which is why operators leave evidence rooms backwards,
with their hands visible.

<details>
<summary>&gt; ping handler</summary>

Which state are you in? You don't know, do you. You're typing words
that land as orders, or orders that land as words, and the screen is
doing exactly what you told it instead of what you meant. Stop
typing. `Esc` answers the question — it puts you in command state no
matter where you were. Then leave the way that discards.

</details>

<details>
<summary>&gt;&gt; ping again</summary>

`vim` opens the file. If the screen ever fills with things you
didn't intend: `Esc`, then `:q!`, then Enter — that leaves and
throws away every change, which right now is exactly what you want.
Walk back in and read again. The file only changes when *you* write
it, and `:q!` never writes.

</details>

<details>
<summary>&gt;&gt;&gt; just tell me</summary>

Fine.

```
vim /var/sigstop/drop/transmission_07.msg
```

Inside: arrow keys to read. Then `Esc`, `:q!`, Enter. That's the
whole visit. `:q!` means quit without saving — the exclamation mark
is you telling vim you mean it. Say the door back to me: `Esc` gets
you to command state, `:q!` burns your changes and leaves. Last
time I explain it.

</details>
