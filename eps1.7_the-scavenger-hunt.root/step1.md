The job brief is on the box: `job.brief`, in your home directory. It has
four clues. Each clue leads to one fragment. Each fragment file tells you
its position and its word:

```
[fragment N/4: word]
```

Recover all four. Join the words with hyphens, all lowercase, in fragment
order. That's the passphrase.

Then send the signal the way you always have: **create a file in your home
directory named exactly the passphrase.**

No doctrine reminders tonight. You either have the habits or you don't.

<details>
<summary>&gt; ping handler</summary>

You're wandering. Stop. This is a recovery op, not a stroll — operators
work the brief. One clue at a time, in order, and each clue tells you
*what kind* of hidden you're dealing with: hidden by a name, hidden inside
noise, hidden by location, hidden behind a door. You've beaten every one
of those before. Match the clue to the episode where you beat it.

</details>

<details>
<summary>&gt;&gt; ping again</summary>

Fine. Tools, not commands.

Clue 1 is the kind of hidden that `ls` lies about — you know the flag
that makes it tell the truth. Clue 2 is one line inside thousands; that's
`grep`, and it has a flag for searching whole directories. Clue 3 is a
filename with no address; that's `find`, starting from `/`, and pipe the
permission-noise somewhere you've heard of: `2>/dev/null` sends errors to
nowhere. Clue 4 isn't hidden at all anymore. Walk in.

Assembly is exact: hyphens, lowercase, fragment order. `pwd` before you
plant the signal file. It has to be home.

</details>

<details>
<summary>&gt;&gt;&gt; just tell me</summary>

No.

This is the job. Everything you need, you already have — scroll up
through your own history if you have to. That's not cheating. That's what
operators do.

I'll be here when it's done.

</details>
