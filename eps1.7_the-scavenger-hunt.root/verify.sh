#!/bin/bash
# eps1.7_the-scavenger-hunt — verify (BOSS)
# One outcome: the signal file, named the assembled passphrase, in home.
# Proves all four recoveries and correct assembly. Route doesn't matter.

if [ -e "/root/everyone-sees-nobody-looks" ]; then
  exit 0
fi

exit 1
